# sdl3stris
Havent used C in a while and wanted to retry an old challenge. Focused on making it look good because thats fun

# Dependencies
- sdl3
    ```
    sudo pacman -S sdl3
    sudo apt install libsdl3-dev
    brew install sdl3
    ```

- gnu make
    ```
    sudo apt install make
    sudo pacman -S make
    brew install make 
    ```

# Progress
Thought it was fun to see the primitives coming together.

